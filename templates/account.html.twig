{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {% include 'block/schedule.html.twig' with {'group': group} %}
    </div>
    <div>
        {% include 'block/grades.html.twig' with {'group': group} %}
    </div>    
    <div>
        {% include 'block/profile.html.twig' with {'user': user} %}
    </div>
{% endblock %}

