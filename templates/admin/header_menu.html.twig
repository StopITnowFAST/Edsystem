{% extends 'base.html.twig' %}

{% block title %}Пункты меню | {{ app_title }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('/styles/admin_tables.css') }}">
    <link rel="stylesheet" href="{{ asset('/styles/pagination.css') }}">
    <link rel="stylesheet" href="{{ asset('/styles/popup.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="admin-table__wrap">
            <div class="admin-table__buttons">
                <h1>Пункты меню</h1>
                <a href="{{ path('admin_create_header-menu') }}" class="btn btn-primary" onclick="openPopup()">Добавить файл</a>
            </div>
            <div class="admin-table__body">
                <table class="admin-table__table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Название</th>
                            <th>Родительский элемент</th>
                            <th>Уровень вложенности</th>
                            <th>Ссылка</th>
                            <th>Порядок</th>
                            <th>Статус</th>
                            <th>Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for note in notes %}
                            <tr>
                                <td>{{ note.id }}</td>
                                <td>{{ note.name }}</td>
                                <td>{{ note.parentId }}</td>
                                <td>{{ note.ItemLevel }}</td>
                                <td>{{ note.url }}</td>
                                <td>{{ note.placeOrder }}</td>
                                <td>{{ note.status }}</td>
                                <td></td>
                                {# <td>
                                    <a href={{ path('admin_group', {id: note.id}) }}>Редактировать</a></td>
                                </tr> #}
                        {% endfor %}
                    </tbody>
                </table>    
            </div>
        </div>     
    </div>
    <div class="overlay"></div>
{% endblock %}

{% block javascripts %}
    {# <script src="{{ asset('/js/admin_groups.js') }}"></script> #}
{% endblock %}