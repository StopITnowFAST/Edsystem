{% extends 'base.html.twig' %}

{% block title %}Преподаватели | Интеллектуальный чат Энергомера{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('/styles/teachers.css') }}">
    <link rel="stylesheet" href="{{ asset('/styles/pagination.css') }}">
    <link rel="stylesheet" href="{{ asset('/styles/popup.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="teachers__wrap">
            <div class="teachers__head">
                <div class="teachers__buttons">
                    <button class="btn btn-primary" onclick="openPopup()">Добавить преподавателя</button>
                </div>
            </div>
            <div class="teachers__body">
                <table class="teachers__table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Фамилия</th>
                            <th>Имя</th>
                            <th>Отчество</th>
                            <th>Должность</th>
                            <th>Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for note in notes %}
                            <tr>
                                <td>{{ note.id }}</td>
                                <td>{{ note.last_name }}</td>
                                <td>{{ note.first_name }}</td>
                                <td>{{ note.middle_name }}</td>
                                <td>{{ note.position }}</td>
                                <td>
                                    <a href={{ path('admin_user_card', {id: note.id}) }}>Редактировать</a></td>
                                </tr>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>    
            </div>
        </div>     
    </div>          
    <div class="overlay"></div>
    {% include('blocks/pagination.html.twig') %}
    {% include('admin/popup/popup_add_teacher.html.twig') %}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('/js/admin_teachers.js') }}"></script>
{% endblock %}